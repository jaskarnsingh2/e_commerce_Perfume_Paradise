// Load ActiveAdmin dependencies
import 'jquery'
import 'jquery_ujs'

// Initialize ActiveAdmin
document.addEventListener('DOMContentLoaded', () => {
  if (typeof jQuery !== 'undefined') {
    jQuery(() => {
      // ActiveAdmin initialization code if needed
    });
  }
});