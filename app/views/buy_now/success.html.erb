<div class="w-full flex flex-col items-center md:pt-8">
  <h2 class="text-3xl font-semibold text-red mb-4">Congratulations! You purchased your product successfully🎉</h2>

  <h1 class="text-5xl text-indigo-900 text-center font-qalbite"> <%= @product.name %> </h1>
  <div class="max-w-2xl w-full"> 
    <%= image_tag(@product.images.first, class: "w-full h-96 object-contain rounded-lg") if @product.images.any? %>
    <div class="text-gray-300 text-lg mt-4"> 
      <%= @product.description %>
    </div>
  </div>
</div>
